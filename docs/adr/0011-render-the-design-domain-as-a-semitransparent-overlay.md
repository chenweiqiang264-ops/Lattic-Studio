# Render the design domain as a semitransparent overlay

The design-domain implicit body remains independently visible as a semitransparent overlay, while lattice results use opaque materials. All visible implicit layers share the interactive GPU section clipping plane, and the domain can still be hidden or made opaque for inspection.
