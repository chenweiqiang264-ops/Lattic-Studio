# Render the design domain as an opaque layer

The design-domain implicit layer is rendered with full opacity. This supersedes ADR-0011's default semitransparent overlay. Internal TPMS structures are inspected by hiding the domain, selecting a lattice-only view, or using the GPU section plane.
