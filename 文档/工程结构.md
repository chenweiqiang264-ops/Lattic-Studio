shoe_lattice_system/
├── main.py                          # 程序入口
├── config.py                        # 全局配置
├── requirements.txt
│
├── core/                            # 核心算法层（纯计算，无UI）
│   ├── __init__.py
│   │
│   ├── implicit/                    # 隐式曲面核心
│   │   ├── __init__.py
│   │   ├── base.py                  # 基类：ImplicitFunction
│   │   ├── tpms.py                  # TPMS族：Gyroid, Diamond, etc.
│   │   ├── voronoi.py               # Voronoi隐式（从voronoi_implicit_new.py）
│   │   ├── tile.py                  # Tile单元
│   │   └── sdf.py                   # SDF工具（未来扩展）
│   │
│   ├── geometry/                    # 几何操作
│   │   ├── __init__.py
│   │   ├── mesh_ops.py              # 网格操作（从geometry.py拆分）
│   │   ├── boolean_ops.py           # 布尔运算
│   │   ├── marching_cubes.py        # 等值面提取
│   │   └── mesh_utils.py            # 工具函数
│   │
│   ├── field/                       # 场论相关（新增，为未来准备）
│   │   ├── __init__.py
│   │   ├── scalar_field.py          # 标量场基类
│   │   ├── gradient_field.py        # 梯度场
│   │   └── adaptive_field.py        # 自适应场
│   │
│   └── cpp/                         # C++扩展
│       ├── __init__.py
│       ├── distance_field.pyd
│       └── mesh_slicer.pyd
│
├── domain/                          # 业务逻辑层（领域模型）
│   ├── __init__.py
│   │
│   ├── lattice/                     # 晶格生成业务
│   │   ├── __init__.py
│   │   ├── generator.py             # 晶格生成器（整合core/implicit）
│   │   ├── parameters.py            # 参数管理
│   │   └── presets.py               # 预设参数
│   │
│   ├── region/                      # 区域管理业务
│   │   ├── __init__.py
│   │   ├── region.py                # Region类（从region_manager.py）
│   │   ├── plane_region.py          # PlaneRegion类（从plane_region_manager.py）
│   │   ├── region_manager.py        # 区域管理器
│   │   └── adaptive_region.py       # 自适应区域（新增）
│   │
│   ├── slicer/                      # 分割业务
│   │   ├── __init__.py
│   │   ├── mesh_slicer.py           # 分割器（现有）
│   │   └── slice_result.py          # 分割结果封装
│   │
│   └── project/                     # 工程管理业务
│       ├── __init__.py
│       ├── project.py               # Project类
│       ├── project_manager.py       # 工程管理器（现有）
│       └── cache_manager.py         # 缓存管理（从cache_info.py）
│
├── ui/                              # UI层（PyQt界面）
│   ├── __init__.py
│   │
│   ├── main_window.py               # 主窗口（从ui_app.py拆分）
│   │   # 包含：窗口框架、菜单栏、工具栏、splitter布局
│   │
│   ├── panels/                      # 功能面板（从ui_app.py拆分）
│   │   ├── __init__.py
│   │   ├── base_panel.py            # 面板基类
│   │   ├── lattice_panel.py         # 晶格生成面板
│   │   ├── slice_panel.py           # 模型分割面板
│   │   ├── region_panel_a.py        # 区域设计A面板
│   │   └── region_panel_b.py        # 区域设计B面板
│   │
│   ├── viewers/                     # 3D视图
│   │   ├── __init__.py
│   │   ├── base_viewer.py           # 抽象基类（从viewer.py）
│   │   ├── pyvista_viewer.py        # PyVista实现（现有）
│   │   └── open3d_viewer.py         # Open3D实现（现有）
│   │
│   ├── dialogs/                     # 对话框
│   │   ├── __init__.py
│   │   ├── startup_dialog.py        # 启动对话框（现有）
│   │   ├── parameter_dialog.py      # 参数设置对话框
│   │   └── export_dialog.py         # 导出对话框
│   │
│   └── widgets/                     # 自定义控件
│       ├── __init__.py
│       ├── toolbar.py               # 工具栏组件
│       ├── log_widget.py            # 日志控件
│       └── parameter_widgets.py     # 参数输入控件
│
├── utils/                           # 工具模块
│   ├── __init__.py
│   ├── file_utils.py                # 文件操作
│   ├── math_utils.py                # 数学工具
│   └── logger.py                    # 日志工具
│
├── resources/                       # 资源文件
│   ├── icons/
│   ├── styles/
│   └── presets/                     # 预设配置
│
└── tests/                           # 测试（新增）
    ├── test_implicit/
    ├── test_geometry/
    └── test_region/
